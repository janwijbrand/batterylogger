# Installed into the Pi's venv (piwheels provides ARMv6 wheels).
# The dashboard + e-ink render are Go now (see webserver history / eink/); the
# only Python left is the BLE logger, which just needs bleak.
bleak
aiobmsble
