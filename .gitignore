# Python
__pycache__/
*.py[cod]
venv/
.venv/

# Runtime data (lives on the Pi, not in git)
*.db
*.db-wal
*.db-shm

# Local, machine-specific Claude settings
.claude/settings.local.json

# OS cruft
.DS_Store

# compiled Go binaries (build with the respective build.sh)
batterylogger/webserver/batteryweb-go
batterylogger/eink/batteryeink

# debug PNGs from the e-ink renderer (the -png flag)
batterylogger/eink/*.png
